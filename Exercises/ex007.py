#read two scores and show the average
n1 = float(input('Type the first score: '))
n2 = float(input('Type the second score: '))

print('The average of {:.1f} and {:.1f} is {:.1f}.'.format(n1, n2, (n1+n2)/2))